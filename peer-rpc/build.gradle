description = 'peer-rpc'

if ("${analysis}" == "true") {
    checkstyleMain.ignoreFailures = false
}

dependencies {
    compile project(':rpc')
    compile libs.gwt_servlet
    compile libs.guava
    compile libs.openpojo

    testCompile libs.junit
    testCompile project(":test-util")
}
